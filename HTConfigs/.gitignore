# It seems like this ignore file here is importat, because git does not track
# directories only files and this `HTConfigs` directory had otherwise no files,
# so a top-level .gitignore did not work

*                   # ignore everything in this directory and its subdirectories
